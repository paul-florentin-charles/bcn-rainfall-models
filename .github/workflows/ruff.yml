name: Ruff

on: push

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: chartboost/ruff-action@v1
        
      - name: Analyse code with ruff
        run: |
          ruff check $(git ls-files)